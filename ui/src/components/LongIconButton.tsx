/**
 * Takes an icon, a button text, a function that is used on click, and a
 * boolean parameter for whether the border is solid or dashed.
 */
export const LongIconButton = () => {
  return <></>;
};
